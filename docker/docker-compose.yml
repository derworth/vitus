services:
    vitus:
        build:
            context: ../
            dockerfile: docker/Dockerfile
        environment:
            NAME: "vitus"
            EMAIL: "vitus@example.com"
            PASSWORD: "password"
            APP_KEY: "base64:UodiJrx3DkcMlizmoimNlDn+yd4q5f2VbkBay19rJwM="
            APP_PORT: 8000
        extra_hosts:
            - 'host.docker.internal:host-gateway'
        ports:
            - '${APP_PORT:-80}:80'
            - '${VITE_PORT:-5173}:${VITE_PORT:-5173}'
        volumes:
            - "vitus-storage:/var/www/html/storage"
volumes:
    vitus-storage:
        driver: local
